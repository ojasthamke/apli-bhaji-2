import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.aplibhaji.app',
    appName: 'APLI BHAJI',
    webDir: 'dist',
    server: {
        androidScheme: 'https'
    }
};

export default config;
